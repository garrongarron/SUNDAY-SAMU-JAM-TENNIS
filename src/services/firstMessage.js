import game from "./Game.js";
import nick from "./nick.js";

const firstMessage = (conn, peerId) => {
    // conn.send({ game: {
    //     statusGame: game.getState()
    // } })
    // console.log(`enviando primer mensaje a ${peerId}`);
}

export default firstMessage